
    <% layout("/layouts/boilerplate") %>

    <style>
        body {
            background-color: #f8f9fa;
        }

        .booking-card {
            max-width: 500px;
            margin: 50px auto;
            padding: 30px;
            background: white;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            font-size: 20px;
            font-weight: bold;
            text-align: center;
            padding: 15px;
            border-radius: 10px 10px 0 0;
        }

        .card-body {
            font-size: 18px;
        }

        .list-group-item {
            font-weight: bold;
            border: none;
            font-size: 16px;
        }

        .icon {
            margin-right: 10px;
            color: #007bff;
        }
    </style>


    <div class="container">
        <div class="booking-card">
            <div class="card">
                <div class="card-header">
                    <i class="fas fa-check-circle"></i> Your Booking Information
                </div>
                <div class="card-body">
                    <ul class="list-group">
                        <li class="list-group-item"><i class="fas fa-user icon"></i> Name: <%= book.name %></li>
                        <li class="list-group-item"><i class="fas fa-briefcase icon"></i> Service: <%= book.service %></li>
                        <li class="list-group-item"><i class="fas fa-map-marker-alt icon"></i> Address: <%= book.address %></li>
                        <li class="list-group-item"><i class="fas fa-clock icon"></i> Time: <%= book.time %></li>
                        <li class="list-group-item"><i class="fas fa-exclamation-triangle icon"></i> Problem: <%= book.problem %></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

   
    -->

    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card shadow-lg">
            <div class="card-body">
                <h2 class="card-title text-center">Your Booking Information</h2>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item"><strong>Name:</strong> <%= book.name %></li>
                    <li class="list-group-item"><strong>Service:</strong> <%= book.service %></li>
                    <li class="list-group-item"><strong>Address:</strong> <%= book.address %></li>
                    <li class="list-group-item"><strong>Time:</strong> <%= book.time %></li>
                    <li class="list-group-item"><strong>Problem:</strong> <%= book.problem %></li>
                </ul>
                <form method="POST" action="/booking/<%= book._id %>?_method=DELETE" class="mt-3 text-center">
                    <button class="btn btn-danger">Cancel</button>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

