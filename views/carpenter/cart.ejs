<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>Your All booking list</h2>
    <ul>
        <% for(let book of allbook){ %>
            <li>your booking = <a href="/booking/<%= book._id %>"><%= book.name %><%= book.service %></a><br></li>
             <br>
         <% } %>
       
    </ul>
</body>
</html> -->

<% layout("/layouts/boilerplate") %>

    <!-- <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
        }

        .cart-item {
            background: white;
            border-radius: 10px;
            padding: 15px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            transition: transform 0.2s ease-in-out;
        }

        .cart-item:hover {
            transform: scale(1.02);
        }

        .cart-img {
            width: 120px;
            height: 120px;
            border-radius: 10px;
            object-fit: cover;
            margin-right: 20px;
        }

        .cart-details {
            flex-grow: 1;
        }

        .cart-actions {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
        }

        .btn-remove {
            background-color: #dc3545;
            color: white;
            border-radius: 5px;
            padding: 5px 10px;
            font-size: 14px;
            text-decoration: none;
            transition: background 0.3s ease;
        }

        .btn-remove:hover {
            background-color: #a71d2a;
            color: white;
        }

        .btn-details {
            background-color: #007bff;
            color: white;
            border-radius: 5px;
            padding: 6px 12px;
            font-weight: bold;
            text-decoration: none;
            transition: background 0.3s ease;
        }

        .btn-details:hover {
            background-color: #0056b3;
            color: white;
        }

        .icon {
            margin-right: 8px;
            color: #007bff;
        }
    </style>


    <div class="container">
        <h2 class="text-center mb-4"><i class="fas fa-shopping-cart"></i> Your Booking Cart</h2>

        <% for(let book of allbook) { %>
            <div class="cart-item">
                <img src="https://via.placeholder.com/120" alt="Service Image" class="cart-img">
                
                <div class="cart-details">
                    <h5><i class="fas fa-user icon"></i> <%= book.name %></h5>
                    <p><strong><i class="fas fa-tools icon"></i> Service:</strong> <%= book.service %></p>
                    <p><strong><i class="fas fa-map-marker-alt icon"></i> Address:</strong> <%= book.address %></p>
                    <p><strong><i class="fas fa-clock icon"></i> Time:</strong> <%= book.time %></p>
                    <p><strong><i class="fas fa-exclamation-triangle icon"></i> Problem:</strong> <%= book.problem %></p>
                </div>

                <div class="cart-actions">
                    <a href="/booking/<%= book._id %>" class="btn btn-details mb-2"><i class="fas fa-eye"></i> View Details</a>
                    <form method="POST" action="/booking/<%= book._id %>?_method=DELETE">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-remove"><i class="fas fa-trash"></i> Remove</button>
                    </form>
                </div>
            </div>
        <% } %>

    </div>

   -->

    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
        }

        .booking-card {
            max-width: 800px;
            margin: auto;
            padding: 20px;
            background: white;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card {
            border: none;
            transition: transform 0.2s ease-in-out;
        }

        .card:hover {
            transform: scale(1.02);
        }

        .card-header {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            font-size: 20px;
            font-weight: bold;
            text-align: center;
            padding: 15px;
            border-radius: 10px 10px 0 0;
        }

        .list-group-item {
            font-weight: bold;
            border: none;
            font-size: 16px;
        }

        .icon {
            margin-right: 10px;
            color: #007bff;
        }

        .btn-details {
            background-color: #007bff;
            color: white;
            border-radius: 20px;
            padding: 10px 20px;
            font-weight: bold;
            text-decoration: none;
            transition: background 0.3s ease;
        }

        .btn-details:hover {
            background-color: #0056b3;
            color: white;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2 class="text-center mb-4">📋 Your Booking List</h2>

        <div class="row">
            <% for(let book of allbook) { %>
                <div class="col-md-6 mb-4">
                    <div class="card">
                        <div class="card-header">
                            <i class="fas fa-calendar-check"></i> Booking Details
                        </div>
                        <div class="card-body">
                            <p><strong><i class="fas fa-user icon"></i> Name:</strong> <%= book.name %></p>
                            <p><strong><i class="fas fa-tools icon"></i> Service:</strong> <%= book.service %></p>
                            <a href="/booking/<%= book._id %>" class="btn btn-details"><i class="fas fa-eye"></i> View Details</a>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

   